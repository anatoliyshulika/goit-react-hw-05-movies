import styled from 'styled-components';
import { color, space, layout, border, flexbox, position } from 'styled-system';

const Box = styled.div`
  ${color}
  ${space}
  ${layout}
  ${border}
  ${flexbox}
  ${position}
`;

export default Box;
